const component = require('./component');
const product = require('./product');
const supplier = require('./supplier');
const component_product = require('./component_product');

module.exports = {
  component,
  product,
  supplier,
  component_product,
};
